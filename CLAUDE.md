# CLAUDE.md

Read **`AGENTS.md`** first. It is the shared instruction file for every AI agent on this project (Claude, Codex, …) and contains the rules, the workflow and the handoff protocol.

Then read **`STATUS.md`** for the next task.

As Claude, your version letter is **A** (e.g. `v0.2.0-A`).
